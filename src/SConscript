# This file tells scons what files make up SWEGUI

import sys

Import('env')

sourceFiles = ['main.cpp']

for i in sourceFiles:
    env.src_files.append(env.Object(i))

# Define tests
env.CxxTest('SWEGUITests', ['unit_tests/SWEGUITests.t.h'])

Export('env')
